Drop future Arabic/English brand font files here.

Recommended direction:
- One readable Arabic family with multiple weights
- One matching Latin family or a unified multilingual family

The app currently uses a safe system fallback so no extra font dependency is required yet.
