Place your real production branding files here:

- `app-icon.png`
- `adaptive-icon.png`
- `splash-logo.png`
- `header-logo.png`

The current files are lightweight placeholders so the app can run now.

If you later add brand fonts, place them in `assets/fonts/` and wire them through the font-loading layer before replacing the system fallback.
